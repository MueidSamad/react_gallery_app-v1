/**
 * PageNotFound component displays a 404 error message
 * Used when users navigate to non-existent routes
 */
const PageNotFound = () => (
  // Styled list item with error message
  <li className="not-found">
    <h1>Error 404: Page Not Found</h1>
  </li>
);

export default PageNotFound;
